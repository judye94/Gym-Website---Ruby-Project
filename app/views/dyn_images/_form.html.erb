<%= form_for @dyn_image, html: { multipart: true } do |f| %>
  <% if dyn_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dyn_image.errors.count, "error") %> prohibited this dyn_image from being saved:</h2>

      <ul>
      <% dyn_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: 'form-control-label' %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :alttext, class: 'form-control-label' %>
    <%= f.text_field :alttext, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :caption, class: 'form-control-label' %>
    <%= f.text_field :caption, class: 'form-control' %>
  </div>
  
  <div class="form-group">
    <%= f.label :image, class: 'form-control-label' %>
    <%= f.file_field :image, class: 'form-control-label' %>
  </div>

  <div class="form-group">
    <%= f.label :pagename, class: 'form-control-label' %>
    <%= f.text_field :pagename, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
